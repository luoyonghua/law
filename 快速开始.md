# 文书管理系统 - 快速开始

## 一、项目启动

### 1. 安装依赖

```bash
pnpm install
```

### 2. 启动开发服务器

```bash
pnpm dev
```

项目将在 `http://localhost:3006` 启动

### 3. 登录系统

使用现有的 Mock 用户登录：
- 超级管理员: `super` / `123456`
- 管理员: `admin` / `123456`
- 普通用户: `user` / `123456`

## 二、功能入口

登录后，在左侧菜单找到"文书管理"模块：

```
文书管理
├── 文书列表    (/documents/list)
└── 批量提取    (/documents/extract)
```

## 三、快速体验

### 场景1: 上传文书

1. 进入"文书列表"页面
2. 点击"上传文书"按钮
3. 拖拽或选择文件（支持 PDF、Word、图片）
4. 点击"开始上传"
5. 等待解析完成（实时显示进度）

### 场景2: 结构化提取

1. 在文书列表中勾选需要提取的文书
2. 点击"批量提取"按钮
3. 等待提取完成
4. 查看提取结果（案号、被告人、罪名、事实等）
5. 可导出结果

### 场景3: 文书比对

1. 在文书列表中勾选至少2份文书
2. 点击"文书比对"按钮
3. 选择比对类型（自定义或预设）
4. 点击"开始比对"
5. 查看比对结果：
   - 相似度统计
   - 差异详情
   - 风险评估
   - 处理建议

## 四、API 配置

### 开发环境

API 地址已配置为：`https://law_cwd.app.xdo.icu:443`

如需修改，编辑 `.env.development` 文件：

```env
VITE_API_URL = https://your-api-url.com
```

### 生产环境

编辑 `.env.production` 文件配置生产环境 API 地址。

## 五、目录结构

```
src/
├── api/
│   └── documents.ts              # 文书 API
├── types/
│   └── api/
│       └── documents.d.ts        # 类型定义
├── views/
│   └── documents/
│       ├── list/                 # 文书列表
│       ├── extract/              # 批量提取
│       └── components/           # 组件
│           ├── UploadDialog.vue
│           ├── ExtractDialog.vue
│           └── CompareDialog.vue
└── router/
    └── modules/
        └── documents.ts          # 路由配置
```

## 六、核心 API

### 1. 获取文档列表
```typescript
import { fetchDocumentList } from '@/api/documents'

const data = await fetchDocumentList()
```

### 2. 批量上传
```typescript
import { uploadDocuments } from '@/api/documents'

const formData = new FormData()
formData.append('files', file)
const response = await uploadDocuments(formData)
```

### 3. 查询进度
```typescript
import { fetchBatchStatus } from '@/api/documents'

const status = await fetchBatchStatus(batchId)
```

### 4. 结构化提取
```typescript
import { batchStructuredExtract } from '@/api/documents'

const result = await batchStructuredExtract(['doc_id1', 'doc_id2'])
```

### 5. 文书比对
```typescript
import { compareDocuments } from '@/api/documents'

const comparison = await compareDocuments(['doc_id1', 'doc_id2'], 'custom')
```

## 七、常见问题

### Q1: 启动失败？
```bash
# 清理依赖重新安装
rm -rf node_modules
pnpm install
```

### Q2: API 请求失败？
- 检查 `.env.development` 中的 API 地址
- 确认后端服务是否正常运行
- 检查网络连接和跨域配置

### Q3: 上传文件失败？
- 确认文件格式正确（PDF、Word、图片）
- 检查文件大小（建议 < 10MB）
- 查看浏览器控制台错误信息

### Q4: 找不到菜单？
- 确认已登录
- 检查用户权限（需要 R_USER 及以上权限）
- 刷新页面重新加载菜单

## 八、开发命令

```bash
# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览生产版本
pnpm serve

# 代码检查
pnpm lint

# 代码格式化
pnpm fix

# 清理开发缓存
pnpm clean:dev
```

## 九、浏览器支持

- Chrome (推荐)
- Edge
- Firefox
- Safari

建议使用最新版本的现代浏览器。

## 十、技术栈

- Vue 3.5+
- TypeScript 5.6+
- Element Plus 2.11+
- Vite 7.1+
- Pinia 3.0+
- Vue Router 4.5+

## 十一、相关文档

- [详细使用指南](./DOCUMENT_MANAGEMENT_GUIDE.md)
- [实现说明](./文书管理系统实现说明.md)
- [API 文档](./API文档.md)
- [界面要求](./前端界面要求.md)

## 十二、获取帮助

如遇到问题：
1. 查看浏览器控制台错误信息
2. 检查网络请求是否正常
3. 查阅相关文档
4. 联系技术支持

---

**祝您使用愉快！** 🎉
